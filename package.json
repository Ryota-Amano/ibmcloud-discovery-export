{
  "name": "ibmcloud-discovey-export",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node ./dist/app.js",
    "build": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "csv-writer": "^1.6.0",
    "ibm-watson": "^6.0.3",
    "js-yaml": "^4.0.0",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.0"
  }
}
